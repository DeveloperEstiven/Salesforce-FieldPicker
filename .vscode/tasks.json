{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile, Lint, Deploy - Current File",
      "type": "shell",
      "command": "bash pre-deploy.sh ${file}",
      "group": "build"
    }
  ]
}
